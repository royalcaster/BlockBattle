using UnityEngine;

namespace BlockBattle
{
    /// <summary>
    /// Enumeration of available block types in BlockBattle.
    /// </summary>
    public enum BlockType
    {
        Cube,
        Cylinder,
        Triangle,
        Rectangle,
        Arch,
        BigTriangle
    }
}

